// son tres operadores logicos, AND, OR, NOT

// and se expresa &&

let mayor = false;
let suscrito = true;

// AND
// operador logico && los dos tiene que ser verdadero, para dar true

console.log('operador AND', mayor && suscrito);

// OR || con que uno sea verdadero da true

console.log('operador OR', mayor || suscrito);

//NOT !antes de la variable

console.log('operador NOT', !mayor);

let soloCatalogoInfantil = !mayor;